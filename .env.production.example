# =============================================================================
# PRODUCTION (Amplify Hosting) — set these as Amplify Environment variables
# Vite bakes VITE_* into the client bundle at build time.
# =============================================================================

VITE_API_URL=https://api.namecardscan.com
VITE_CONTACT_STORAGE=indexeddb

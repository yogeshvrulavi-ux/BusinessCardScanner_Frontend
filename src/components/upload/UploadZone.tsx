import { Upload } from "lucide-react";
import type { DragEvent } from "react";
import { Button } from "@/components/common/Button";
export const UploadZone = ({ isDragging, error, onDragOver, onDragLeave, onDrop, onPick }: { isDragging: boolean; error?: string; onDragOver: (event: DragEvent<HTMLDivElement>) => void; onDragLeave: (event: DragEvent<HTMLDivElement>) => void; onDrop: (event: DragEvent<HTMLDivElement>) => void; onPick: () => void }) => (<div onDragOver={onDragOver} onDragLeave={onDragLeave} onDrop={onDrop} className={`rounded-sm border-2 border-dashed p-8 text-center transition ${isDragging ? "border-primary bg-primary/5" : "border-border bg-muted/20"}`}><div className="mx-auto mb-4 inline-flex h-12 w-12 items-center justify-center rounded-sm bg-gradient-primary text-primary-foreground"><Upload className="h-5 w-5" /></div><p className="text-sm font-medium">Drag and drop business card</p><p className="mt-1 text-xs text-muted-foreground">PNG/JPG up to 10MB</p><Button className="mt-5 rounded-sm" onClick={onPick}>Choose image</Button>{error ? <p className="mt-3 text-xs text-destructive">{error}</p> : null}</div>);
